/**
 * Re-exports the central API object and image base URL.
 *
 * This allows other parts of the application to import API helpers and
 * image URL configuration from a single shared entry point.
 */
export { api, IMAGE_BASE_URL } from "./api/index";
